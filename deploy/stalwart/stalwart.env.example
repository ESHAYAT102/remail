MAIL_HOSTNAME=mail.redakt.app

# Bootstrap only. Generate a long password, use this value for the first-start
# wizard, then remove the variable by recreating with compose.production.yml.
STALWART_RECOVERY_ADMIN=admin:replace-with-a-long-random-password
